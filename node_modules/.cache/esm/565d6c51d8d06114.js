let Jwt;_d8d‍.x([["default",()=>_d8d‍.o]]);_d8d‍.w("jsonwebtoken",[["default",["Jwt"],function(v){Jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if (!token) return next(); // 토큰이 없는 경우
    try {
        const decoded = Jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id: decoded._id,
            username: decoded.username,
        };
        _d8d‍.g.console.log(decoded);
        return next();
    } catch (e) {
        // 토큰 검증 실패
        return next();
    }
};

_d8d‍.d(jwtMiddleware);
