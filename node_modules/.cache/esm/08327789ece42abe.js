let Router,postsCtrl,checkLoggedIn;_ece‍.x([["default",()=>_ece‍.o]]);_ece‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_ece‍.w("./posts.ctrl",[["*",null,function(v){postsCtrl=v}]]);_ece‍.w("../../lib/checkLoggedIn",[["default",["checkLoggedIn"],function(v){checkLoggedIn=v}]]);

 // 로그인 했을 시에만 이용하도록 설정

const posts = new Router();

posts.get('/', postsCtrl.list);
posts.post('/', checkLoggedIn, postsCtrl.write);

const post = new Router(); // /api/posts/:id
post.get('/', postsCtrl.read);
post.delete('/', checkLoggedIn, postsCtrl.remove);
post.patch('/', checkLoggedIn, postsCtrl.update);

posts.use('/:id', postsCtrl.checkObjectId, post.routes());

_ece‍.d(posts);
